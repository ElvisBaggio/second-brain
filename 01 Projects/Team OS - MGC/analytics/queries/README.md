# Queries — Biblioteca SQL oficial

Queries auditadas e aprovadas pelo time de dados. Não edite sem revisar impacto.

## Convenção de nome
`[domínio]_[o-que-faz].sql`
Exemplos: `churn_monthly.sql`, `mau_by_cohort.sql`, `mrr_breakdown.sql`

## Convenção de header em cada query

```sql
-- Query: [nome]
-- Propósito: [o que responde]
-- Tabelas: [lista de tabelas usadas]
-- Atualizado: YYYY-MM-DD
-- Autor: [nome]
```

## Index
(Nenhuma query ainda — adicione à medida que consolidar)
